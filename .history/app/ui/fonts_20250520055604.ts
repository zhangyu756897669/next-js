import {Inter} from 'next/font/google';
export const inter = Inter({ subsets: ['latin'] });
export const roboto = Inter({ subsets: ['Lusitana'], weight: '400' });
export const robotoBold = Inter({ subsets: ['Lusitana'], weight: '700' });